#? Config file for btop — managed by chezmoi.
#? To change the color theme, update `theme` in:
#?   ~/.local/share/chezmoi/.chezmoidata/theme.yaml
#? then run `chezmoi apply`.

color_theme = "base16"
